#!/bin/bash

# read -p "please enter the name of your CidrIp range: " varCidreRange
# read -p "Please enter the name of the configuration file (e.g. config.txt):  "  varConfig
# read -p "Please enter the name of the desired s3 bucket name.  \
# The bucket name can be between 3 and 63 characters long, and can contain only lower-case characters, numbers, periods, and dashes.  Additionally, each name must start with a lowercase letter or number and cannot contain underscores, end with a dash, have consecutive periods, or use dashes adjacent to periods:  "  varBucketName
